# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently a stub. It contains only `README.md` (a single-line title: `# flyingdatapig`) and an Apache 2.0 `LICENSE`. There is no source code, build configuration, dependency manifest, or test suite yet.

When real code lands, expand this file with:
- The build, lint, test, and run commands (including how to run a single test).
- The high-level architecture — components that span multiple files, data flow, and module boundaries that aren't obvious from a single file.
- Anything in a future README, `.cursor/rules/`, `.cursorrules`, or `.github/copilot-instructions.md` that future Claude instances should know.

Until then, do not invent commands or architecture; ask the user for direction on what to scaffold.
